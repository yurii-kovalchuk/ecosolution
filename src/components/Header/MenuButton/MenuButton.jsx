import Icon from "../../../assets/menu.svg?react";
import { Button } from "./MenuButton.styled";

const MenuBtn = () => {
  return (
    <Button type="button">
      <Icon />
    </Button>
  );
};

export default MenuBtn;
